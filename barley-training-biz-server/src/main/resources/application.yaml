spring:
  application:
    name: barley-training-biz
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  web:
    resources:
      add-mappings: false
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: ${BARLEY.DATASOURCE.URL:jdbc:mysql://mysql.xxscloud.com:3306/barley_training?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8&SSL=true}
    username: ${BARLEY.DATASOURCE.USERNAME:root}
    password: ${BARLEY.DATASOURCE.PASSWORD:Lxasd123!@#}
  data:
    # Redis 6.X
    redis:
      host: ${BARLEY.REDIS.HOST:r-uf61dmje3oz816jctppd.redis.rds.aliyuncs.com}
      port: ${BARLEY.REDIS.PORT:6379}
      password: ${BARLEY.REDIS.PASSWORD:r-uf61dmje3oz816jctp:0ElTRu1TFRcBHLyk}
      database: ${BARLEY.REDIS.DATABASE:2}
      timeout: 6000
      lettuce:
        pool:
          max-active: 10
          max-idle: 10
          max-wait: 10
          min-idle: 0
        shutdown-timeout: 100ms
  safety:
    ignoring:
      - /auth/**
      - /s3/**

barley:
  filecenter:
    s3:
      config:
        - id: FILES
          accessKey: ${BARLEY.S3.ACCESS_KEY:muOqkcIr0dzUSot4MU5p}
          secretKey: ${BARLEY.S3.ACCESS_SECRET_KEY:LdZpTrwAkxnEPaBCxquhDysXY94eFt1nT2DMqUQk}
          bucket: ${BARLEY.S3.BUCKET:}
          region: ${BARLEY.S3.REGION:shanghai}
          endpoint: ${BARLEY.S3.ENDPOINT:http://192.168.5.33:9000}
          external-endpoint: ${BARLEY.S3.EXTERNAL_ENDPOINT:http://192.168.5.33:9000}